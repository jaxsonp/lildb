
[alias]
run-server = "run --package server --bin lildb-server"
run-cli = "run --package cli --bin lildb-cli"

[profile.dev]

[profile.release]

[profile.test]
inherits = "dev"

[profile.bench]
inherits = "release"